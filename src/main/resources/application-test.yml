spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:jpa-self-study-test
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create
    open-in-view: false
  output:
    ansi:
      enabled: always

server:
  port: 7070
